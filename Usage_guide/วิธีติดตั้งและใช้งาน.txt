เข้าโฟลเดอร์โปรเจ็กต์
----------------
cd SA_Novel
# หรือโฟลเดอร์นิยายเรื่องอื่น


สร้าง Virtual Environment
-----------------------
เพื่อเก็บ Dependencies ไว้ในโปรเจ็กต์นี้เท่านั้น
python -m venv venv
# ถ้าเคยสร้าง venv แล้ว ไม่ต้องสร้างอีก
venv\Scripts\activate


ติดตั้ง Dependencies
-----------------
pip install google-generativeai python-dotenv structlog tqdm flet pyyaml pyinstaller tiktoken
# ทำครั้งแรกครั้งเดียว

set OPENAI_API_KEY=sk-...
# ติดตั้งแบบถาวรใน Environment Variables ของ Windows แล้ว ไม่ต้องทำอีก
# ล่าสุดใช้การติดตั้งแบบถาวรใน Environment Variables ของ Project ไม่ต้องทำซ้ำใน verv นั้น


ตรวจ systex error
----------------
python -m py_compile ip-Trans.py


คำสั่งแปล
----------
📌 วิธีใช้งาน (ตัวอย่าง)
# 1. แปลบทเดียว (เหมือนเดิม)
python ip-Trans.py chapters_src/sa_0001.txt

# 2. แปลบท 1-10
python ip-Trans.py --range 1 10

# 3. แปลแบบข้ามบทที่แปลแล้ว
python ip-Trans.py --range 1 100 --skip-existing

# 4. แปลแบบเงียบ
python ip-Trans.py --range 50 60 --quiet

# 5. ใช้ prefix ต่างกัน
python ip-Trans.py --range 1 20 --prefix novel

# 6. ระบุไฟล์เอง
python ip-Trans.py --files chapters_src/sa_0001.txt chapters_src/sa_0005.txt

💡 ข้อควรระวัง:
อย่าลืมตั้ง GOOGLE_API_KEY ใน .env
หากใช้ --range → ไฟล์ต้องอยู่ใน chapters_src/ และชื่อเป็น sa_0001.txt (หรือตาม --prefix)
ระบบจะ อัปเดต glossary อัตโนมัติ ทุกบท → บทหลังจะได้ benefit จากคำศัพท์ใหม่ของบทก่อน


GUI แก้คำศัพท์
------------
python glossary_editor.py

Reprocess
---------
python reprocess.py chapters_src/sa_0001.txt


Reprocess All Updated
---------------------
# ตรวจสอบก่อนรันจริง
python reprocess_all_outdated.py --dry-run

# รันจริง
python reprocess_all_outdated.py

# บังคับอัปเดตทุกบท
python reprocess_all_outdated.py --force


✅ สรุปสิ่งที่คุณมีตอนนี้
------------------
trans1.py
แปลบทใหม่ + ใช้ glossary + cache + Git versioning
reprocess.py
อัปเดตบทเดียวที่ล้าสมัย
reprocess_all_outdated.py
อัปเดตทุกบท + สร้าง
diff_report.html
glossary_editor.py
GUI แก้ไขศัพท์ + กรองตามวัน/บท + auto git commit

🎁 ต่อไปคุณสามารถ:
-----------------
แปลบทใหม่ → python trans1.py chapters_src/sa_0006.txt
แก้ glossary.yaml ผ่าน GUI → กดบันทึก → auto commit
รันอัปเดตทั้งระบบ → python reprocess_all_outdated.py
ดูรายงานการเปลี่ยนแปลง → เปิด reports/diff_report_*.html


ไฟล์ .env ช่วยติตตั้ง verv ของโปรเจคนี้
ไฟล์ .gitignore ช่วยปกป้องรหัสหรือไฟล์สำคัญจากการเก็บบันทึกลง git
