นี่คือ **เอกสารสรุปอ้างอิง (Reference Summary)**

---

# 📌 **ระบบแปลนิยายจีน → ไทย (ip-Trans) – สรุปสถานะ & แผนพัฒนา**

## ✅ **สิ่งที่เรามีอยู่แล้ว (ปัจจุบัน)**

### 🔧 **เทคโนโลยี & ฟีเจอร์หลัก**
- ใช้ **Python + Google Gemini API** (รองรับ fallback, safety bypass)
- มีระบบ **glossary.yaml** สำหรับควบคุมคำแปลเฉพาะ
- มี **GUI แก้ไข glossary** ด้วย **Flet** (กรองตามวันที่/บท, auto Git commit)
- มีระบบ **reprocess** แบบฉลาด:
  - `reprocess.py` → อัปเดต **บทเดียว**
  - `reprocess_all_outdated.py` → อัปเดต **ทุกบทที่ outdated** + สร้างรายงาน HTML diff
- รองรับ **batch processing** (แปลหลายบท, progress bar, rate limiting)
- ใช้ **Git + metadata** ติดตามเวอร์ชัน glossary และบทแปล
- โครงสร้างโฟลเดอร์แบบ **single-novel** (ทุกอย่างอยู่ในโฟลเดอร์เดียว)

### 🗂️ **โครงสร้างโฟลเดอร์ปัจจุบัน (Single-Novel)**
```
ip-trans/
├── chapters_src/
├── chapters_translated/
├── logs/, backups/, reports/, storage/
├── glossary.yaml
├── ip-Trans.py
├── reprocess.py
├── reprocess_all_outdated.py
├── glossary_editor.py
└── ... (config, .env, etc.)
```

---

## 🎯 **เป้าหมายใหม่: รองรับหลายนิยาย (Multi-Novel)**

### 🔁 **เหตุผล**
- ป้องกันการปนกันของ glossary ระหว่างนิยาย
- จัดการโปรเจกต์ได้เป็นระบบ
- รองรับ GUI รวมในอนาคต

---

## 🛠️ **สิ่งที่เราจะทำ**

### 1. **ปรับโครงสร้างโฟลเดอร์**
```
ip-trans/
├── novels/
│   ├── novel_A/        ← ข้อมูลนิยาย A (โครงสร้างเหมือนโฟลเดอร์รากเดิม)
│   │   ├── chapters_src/
│   │   ├── chapters_translated/
│   │   ├── logs/, backups/, reports/, storage/
│   │   └── glossary.yaml
│   │
│   └── novel_B/        ← ข้อมูลนิยาย B
│
├── ip-Trans.py         ← สคริปต์หลัก (1 ชุด ใช้ได้ทุกนิยาย)
├── reprocess.py
├── reprocess_all_outdated.py
├── glossary_editor.py
├── main_gui.py         ← จะสร้างทีหลัง
└── ... (config กลาง: .env, vertex ai json, venv/)
```

### 2. **ปรับสคริปต์หลักทุกตัวให้รับ `--novel`**
- ทุกสคริปต์จะรับ argument: `--novel NOVEL_NAME`
- สร้าง path แบบไดนามิก:  
  `novels/{novel_name}/chapters_src`, `.../glossary.yaml`, ฯลฯ
- **สคริปต์ที่ต้องแก้ไข**:
  - `ip-Trans.py`
  - `reprocess.py`
  - `reprocess_all_outdated.py`
  - `glossary_editor.py`

> ✅ ใช้แนวทาง **DRY**: สร้าง `path_utils.py` สำหรับจัดการ path รวม

### 3. **ทดสอบ CLI ให้ทำงานได้ก่อน**
ตัวอย่างคำสั่งหลังปรับ:
```bash
# แปลบท
python ip-Trans.py --novel novel_A --range 1 10

# แก้ไข glossary
python glossary_editor.py --novel novel_A

# Reprocess บทเดียว
python reprocess.py --novel novel_A chapters_src/sa_0001.txt

# Reprocess ทุกบท outdated
python reprocess_all_outdated.py --novel novel_A
```

### 4. **สร้าง `main_gui.py` (ขั้นต่อไป)**
- GUI รวมแบบ desktop (ใช้ PySimpleGUI)
- มี 4 ปุ่มหลัก:
  1. 📖 แปลบท → เลือกช่วงบท
  2. 📚 แก้ไข glossary → เปิด Flet editor
  3. 🔁 Reprocess บทเดียว → ใส่เลขบท
  4. 🌐 Reprocess ทุกบท outdated → รันทั้งเรื่อง
- รองรับการเลือกนิยายจาก dropdown

---

## ✅ **หลักการออกแบบ**
- **ไม่คัดลอกสคริปต์ซ้ำ** → สคริปต์หลักมีแค่ 1 ชุด
- **ไม่แก้ไข logic หลัก** → แค่เปลี่ยนวิธีโหลด path
- **ยังใช้ CLI ได้ 100%** → GUI เป็นตัวเรียก CLI เท่านั้น
- **แต่ละนิยายเป็น independent project** → ไม่ปนกัน

---

## 📦 **ผลลัพธ์สุดท้ายที่ต้องการ**
> ระบบแปลนิยายจีน → ไทย ที่:
> - รองรับ **หลายนิยาย** อย่างเป็นระบบ  
> - ใช้งานได้ทั้ง **CLI** (สำหรับ power user) และ **GUI** (สำหรับใช้งานทั่วไป)  
> - **ไม่ซับซ้อนเกินไป** — รักษาความเรียบง่ายของระบบเดิมไว้  
> - **พร้อมขยาย** — เพิ่มนิยายใหม่ได้ทันทีโดยไม่ต้องแตะโค้ด

---

